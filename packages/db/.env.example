# Resend API key (re_…). Read by `supabase config push` to interpolate
# `env(AUTH_RESEND_KEY)` in supabase/config.toml. Not used by app code.
#
# Copy this file to `.env.local` here, paste the key, never commit it.
AUTH_RESEND_KEY=
